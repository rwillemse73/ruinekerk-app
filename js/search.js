// Reisblik 8.1 — Zoekfunctie stap 1
// Alleen de knop wordt toegevoegd. De zoekfunctie zelf is nog niet actief.
document.addEventListener("DOMContentLoaded", () => {
  const button = document.getElementById("searchContentBtn");
  if (!button) return;

  button.addEventListener("click", () => {
    // Stap 1: bewust nog geen zoekfunctie.
    // De knop is aanwezig en reageert niet inhoudelijk.
  });
});
